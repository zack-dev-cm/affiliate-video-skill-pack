{
  "pro_pack_checkout_url": "REPLACE_WITH_PROVIDER_CHECKOUT_URL",
  "setup_review_checkout_url": "REPLACE_WITH_PROVIDER_CHECKOUT_URL",
  "managed_launch_checkout_url": "REPLACE_WITH_PROVIDER_CHECKOUT_URL",
  "provider": "optional_hosted_checkout_override",
  "notes": "Optional override. Leave absent to use the server-side NOWPayments invoice function. Do not commit secrets."
}
